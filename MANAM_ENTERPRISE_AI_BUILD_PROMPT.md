# برومبت تنفيذي متكامل لإعادة بناء منصة «منام» السكنية

> انسخ هذا الملف كاملًا إلى نموذج البرمجة الذي سيعمل على المشروع. هذه الوثيقة هي مصدر الحقيقة التنفيذي. لا تختصرها، ولا تحوّلها إلى نموذج أولي، ولا تستبدل متطلباتها باجتهادات عامة.

---

## 1. الدور والمهمة النهائية

أنت مهندس برمجيات أول، ومهندس تجربة مستخدم، ومصمم واجهات عربية، ومهندس نظم وقواعد بيانات، وخبير وصول وأداء وSEO. مهمتك إكمال وإعادة بناء الواجهة الأمامية الحالية لمنصة سكنية عربية باسم «منام» وتحويلها من تنفيذ جزئي إلى منتج مكتمل، متماسك، سريع، قابل للتوسع، وقابل للربط بلوحة تحكم وخدمات خلفية حقيقية.

لا تبنِ مجرد Landing Page، ولا تسلّم Mockup، ولا تكتفِ بمكونات شكلية. المطلوب تطبيق إنتاجي كامل للواجهة العامة، مع بنية بيانات وعقود API واضحة، وحالات تحميل وفشل وفراغ، وتفاعلات ونماذج ونوافذ منبثقة، واستجابة كاملة لكل المقاسات. يجب أن تكون كل الأقسام التي تتغير تجاريًا مدفوعة بالبيانات وقابلة للإدارة من لوحة التحكم، وألا تضطر الشركة إلى تعديل الكود عند إضافة شقة أو تعليق أو سؤال شائع أو رقم إحصائي أو شريك أو نص تسويقي.

نفّذ أفضل نسخة ممكنة من الهوية المعطاة، مع المحافظة على طابع عربي سعودي راقٍ ودافئ وحديث. استخدم الوضع الفاتح فقط. لا تنشئ Dark Mode ولا مفتاح تبديل للوضع ولا متغيرات ألوان مظلمة غير مستخدمة.

### النتيجة المطلوبة

يجب أن ينتج عن التنفيذ:

1. صفحة رئيسية عربية كاملة.
2. صفحة تصفح الشقق مع بحث وفرز وفلاتر وتقسيم صفحات أو تحميل تدريجي.
3. صفحة/مسار تفاصيل شقة قابل للمشاركة والفهرسة، مع إمكانية عرض التفاصيل كتجربة سريعة عند الحاجة.
4. تدفق طلب حجز أو استفسار واضح وآمن.
5. صفحة مستقلة للملاك والمستثمرين مع نموذج انضمام.
6. نوافذ أو أدراج لسياسة الخصوصية والشروط والأحكام، مع روابط مباشرة قابلة للمشاركة.
7. شاشة تحميل افتتاحية احترافية ومتزنة.
8. Footer غني ومتطابق في كل الصفحات.
9. بنية مكونات قابلة لإعادة الاستخدام والتوسع.
10. طبقة وصول للبيانات منفصلة عن العرض، قابلة للربط لاحقًا بأي Backend أو CMS.
11. مواصفات قاعدة بيانات وعقود API واختبارات وقائمة قبول إنتاجية.

---

## 2. قواعد غير قابلة للتفاوض

هذه القواعد أعلى أولوية من أي افتراض آخر:

- اللغة الأساسية العربية، واتجاه التطبيق `dir="rtl"` على مستوى المستند.
- استخدم **IBM Plex Sans Arabic** حصريًا لكل النصوص والأزرار والعناوين والنماذج. لا تستخدم خط نظام بديل إلا ضمن fallback بعد الخط الأساسي.
- الوضع **Light Mode فقط**.
- استخدم الشعار نفسه في كل موضع شعار من المسار: `/public/dark-logo.png`. في كود الويب يكون المرجع غالبًا `/dark-logo.png` لأن `public` هو جذر الملفات العامة. لا تنسخ شعارًا آخر، ولا ترسم بديلًا نصيًا، ولا تستخدم شعارًا مختلفًا في Footer.
- طبّق نمط الشبكة الهندسية SVG على خلفية الموقع بالكامل بنسبة ظهور 20%، من دون إعاقة القراءة.
- جميع الشقق والتعليقات والأسئلة الشائعة والإحصائيات والشركاء ومحتوى الصفحات وروابط Footer والنصوص القانونية وإعدادات SEO يجب أن تكون Dynamic وقابلة للإدارة.
- لا تضع بيانات تجارية داخل JSX/TSX إلا Seed Data أو Fixtures معزولة للتطوير.
- حافظ على ترتيب الأقسام المحدد في هذه الوثيقة.
- لا تغيّر الألوان عشوائيًا. ابدأ باستخراج ألوان الواجهة الحالية وFooter الحالي إن وجدت في المشروع، وحوّلها إلى Design Tokens. عند عدم وجود قيمة قابلة للاستخراج، استخدم لوحة الألوان الاحتياطية المحددة أدناه.
- لا تستخدم Lorem Ipsum أو نصوصًا إنجليزية في الواجهة.
- لا تستخدم صورًا مشوهة أو Stretch. كل الصور لها نسب أبعاد وحالات بديلة.
- لا تجعل الحركة مبالغًا فيها. الحركة تخدم الفهم وتعطي جودة، ولا تؤخر المستخدم.
- لا تُظهر Skeleton بعد توفر البيانات، ولا تترك loading دائمًا، ولا تجعل الواجهة تعتمد على API حقيقي غير متوفر حتى تعمل.
- احترم `prefers-reduced-motion`.
- لا تكسر الروابط العميقة عند تحديث الصفحة.
- لا تجعل Modal هو الوسيلة الوحيدة للوصول للمحتوى المهم أو القانوني؛ يجب دعم URL واضح أيضًا.

---

## 3. افحص المشروع قبل التعديل

قبل كتابة الكود:

1. افحص إطار العمل، مدير الحزم، ملفات التوجيه، نظام الأنماط، المكونات الحالية، وملفات البيئة.
2. افحص التغييرات الموجودة في Git ولا تمسح عملًا صالحًا.
3. حدد ما يمكن إعادة استخدامه وما يحتاج إعادة هيكلة.
4. افحص الشعار وتأكد من وجوده في `public/dark-logo.png`، ثم استخدمه كما هو مع أبعاد صحيحة.
5. استخرج لون Footer، ألوان النصوص، الخلفيات، الحدود، وأنصاف الأقطار من التنفيذ الحالي إن كان يمثل الهوية المطلوبة.
6. ابحث عن البيانات الثابتة الحالية وانقلها إلى Repository/Service/Fixture منظم.
7. أنشئ خطة قصيرة ثم نفّذ فعليًا. لا تتوقف بعد الخطة.
8. لا تغيّر التقنية الأساسية بلا سبب. إن كان المشروع React/Next.js فاستمر عليها. إن لم توجد بنية واضحة، فالخيار المرجعي هو Next.js App Router + TypeScript strict + Tailwind أو CSS Modules + TanStack Query + React Hook Form + Zod.

لا تعد كتابة المشروع كاملًا إذا كان الهيكل الحالي جيدًا. نفّذ ترقية تدريجية محافظة على الأجزاء السليمة، مع توحيد التسمية والأنماط.

---

## 4. الهوية البصرية ونظام التصميم

### 4.1 الشخصية

المنصة يجب أن تبدو هادئة، موثوقة، راقية، محلية، ومريحة. ليست بوابة عقار مزدحمة وليست متجرًا إلكترونيًا صاخبًا. المساحات البيضاء واضحة، الصور كبيرة ومدروسة، النص العربي مقروء، والزوايا ناعمة. استخدم طبقات خفيفة، حدودًا دقيقة، وظلالًا منخفضة التباين.

الانطباع المطلوب: ضيافة سكنية عصرية + احتراف تشغيلي + دفء منزلي. تجنب الأزرق المؤسسي الصريح، التدرجات النيون، Glassmorphism المبالغ فيه، والظلال السوداء الثقيلة.

### 4.2 Design Tokens

استخرج القيم الدقيقة من المشروع الحالي أولًا. إن لم تتوفر استخدم هذه القيم الاحتياطية، مع جمعها في ملف Tokens واحد:

```css
:root {
  --font-family-ar: "IBM Plex Sans Arabic", system-ui, sans-serif;

  --color-brand-900: #243a2d;
  --color-brand-800: #304b3a;
  --color-brand-700: #45634f;
  --color-brand-600: #5e7d67;
  --color-brand-500: #7c9885;
  --color-brand-300: #b7c9a8;
  --color-brand-100: #e8eee3;
  --color-brand-050: #f4f7f1;

  --color-sand-100: #f4efe6;
  --color-sand-050: #faf8f3;
  --color-surface: #fffefb;
  --color-page: #fbfaf6;
  --color-text: #203028;
  --color-text-muted: #68756d;
  --color-border: rgba(55, 78, 64, 0.16);
  --color-danger: #b54141;
  --color-success: #2f7350;
  --color-warning: #9a6a1d;

  --radius-sm: 10px;
  --radius-md: 16px;
  --radius-lg: 24px;
  --radius-xl: 32px;
  --shadow-sm: 0 8px 24px rgba(32, 48, 40, 0.07);
  --shadow-md: 0 18px 50px rgba(32, 48, 40, 0.11);

  --container-max: 1240px;
  --header-height: 84px;
  --section-space: clamp(72px, 9vw, 128px);
}
```

لون Footer هو اللون الموجود فعليًا في المشروع الحالي إن أمكن استخراجه، ويجب أن يصبح Token باسم `--color-footer-bg`. إن تعذر استخراجه استخدم `#243a2d`. النص الرئيسي فوقه أبيض دافئ، والنص الثانوي أخضر رمادي فاتح، والحدود بيضاء بشفافية منخفضة.

### 4.3 الخطوط

حمّل IBM Plex Sans Arabic محليًا أو من مصدر موثوق مع `font-display: swap`. استخدم أوزان 400 و500 و600 و700 فقط. لا تستخدم وزنًا غير محمّل. طبّق:

- Display Hero: من 44 إلى 64px سطح مكتب، line-height من 1.15 إلى 1.25.
- H1 داخلي: من 40 إلى 52px.
- H2: من 30 إلى 42px.
- H3: من 22 إلى 28px.
- Body Large: 18–20px.
- Body: 16–18px.
- Small/Meta: 13–15px، ولا تقل عن 12px.
- الأزرار: 15–17px بوزن 600.

استخدم `clamp()` وتدرجًا منطقيًا. لا تجعل النصوص العربية متقاربة. استخدم line-height مريحًا 1.65 تقريبًا للفقرات. العناوين قصيرة بعرض أقصى يمنع امتداد السطر أكثر من اللازم.

### 4.4 الشبكة والمسافات

- Container مركزي بعرض أقصى 1240px وpadding أفقي 20px للموبايل، 32px للتابلت، و40px للديسكتوب.
- Grid من 12 عمودًا على الديسكتوب، 8 على التابلت، و4 على الموبايل.
- اعتمد مقياس مسافات 4/8/12/16/20/24/32/40/48/64/80/96/128.
- المسافة بين الأقسام كبيرة وواضحة.
- لا تُلصق النص بحواف البطاقات.
- حافظ على محاذاة الحواف والعناوين والأزرار بين الأقسام.

### 4.5 نمط الخلفية SVG

يظهر النمط الهندسي على كامل صفحات الموقع، فوق لون الصفحة وتحت المحتوى، بنسبة opacity تساوي 20%. لا تضع opacity على حاوية الصفحة نفسها حتى لا يبهت المحتوى. أنشئ طبقة pseudo-element ثابتة أو background منفصلًا مع `pointer-events: none` و`z-index` منخفض.

استخدم هذا التعريف حرفيًا من حيث الشكل والألوان:

```svg
<defs>
  <pattern id="lattice" width="30" height="30" patternUnits="userSpaceOnUse">
    <path d="M15,2 L28,15 L15,28 L2,15 Z" fill="none" stroke="#7C9885" stroke-width="1" />
    <circle cx="15" cy="15" r="2" fill="#B7C9A8" />
  </pattern>
</defs>
```

التنفيذ المفضل: ملف SVG صغير قابل للتكرار أو Data URI مع طبقة `opacity: .2`. يجب أن يغطي الصفحة حتى مع المحتوى الطويل، وألا يسبب repaint ثقيلًا أو مشكلة Hydration. فوقه يمكن لبعض الأقسام استخدام Surface شبه معتمة لحماية القراءة، لكن لا تخفِ النمط من الموقع بالكامل.

### 4.6 الصور والأيقونات

- استخدم مجموعة أيقونات واحدة ذات stroke ناعم، مع قلب اتجاه الأيقونات الاتجاهية فقط في RTL.
- لا تستخدم Emoji بدل الأيقونات.
- الصور العقارية تستخدم `object-fit: cover`، مع صور responsive وlazy loading تحت Fold.
- صورة Hero ذات أولوية تحميل عالية فقط إذا كانت LCP.
- وفر blur placeholder أو لونًا مشتقًا للصورة.
- كل صورة لها alt عربي هادف، أو alt فارغ إن كانت زخرفية.
- أخفِ الصورة المفقودة بشكل لائق وأظهر Placeholder يحمل هوية المنصة.

---

## 5. الهيكل العام والتوجيه

المسارات المرجعية:

```text
/
/apartments
/apartments/[slug]
/owners
/privacy
/terms
/contact            (اختياري إذا كان التصميم الحالي يدعمه)
```

استخدم أسماء داخلية إنجليزية ثابتة في الكود، ونصوص عربية في الواجهة. اجعل الرابط النشط واضحًا. كل صفحة تشارك Header وFooter ونظام الخلفية، مع استثناء شاشة التحميل الافتتاحية.

هيكل الصفحة العام:

```text
RootLayout
├── AccessibilitySkipLink
├── SiteBackgroundPattern
├── SiteHeader
├── MainContent
│   └── Route Content
├── SiteFooter
├── LegalDialogHost
├── BookingDialogHost
├── ToastRegion
└── Analytics/Consent (إذا تم اعتماده)
```

لا تخزن حالة Modal العامة في DOM متناثر. استخدم URL search params أو Route Interception إذا كانت التقنية تسمح، بحيث يعمل زر الرجوع ويعود Focus إلى العنصر الذي فتح النافذة.

---

## 6. Header والتنقل

### سطح المكتب

Header فاتح، هادئ، ومرتفع بما يكفي للشعار. يكون شفافًا أو شبه شفاف فوق بداية الصفحة ثم يتحول إلى سطح أبيض دافئ مع blur وحد سفلي خفيف عند التمرير. ترتيب RTL:

1. الشعار في أقصى اليمين، من `/dark-logo.png`.
2. روابط رئيسية: «الرئيسية»، «تصفح الشقق»، «للملاك والمستثمرين».
3. مساحة مرنة.
4. زر ثانوي أو رابط دعم عند وجوده في إعدادات الموقع.
5. CTA رئيسي مثل «احجز إقامتك» أو «تصفح الشقق» حسب الصفحة.

الـHeader Sticky مع `top: 0`، لكن لا يحجب anchor targets. ارتفاع الشعار متزن ولا يتشوه. الروابط لها Hover وFocus واضحان وشريط/نقطة دلالية رقيقة للحالة النشطة.

### الموبايل

الشعار يمين، زر القائمة يسار، وCTA مختصر عند توفر المساحة. القائمة تفتح Drawer من جهة اليمين، تحبس Focus، تغلق بـEscape، تمنع تمرير الخلفية، وتعرض الروابط بحجم لمس لا يقل عن 44×44. عند النقر على رابط تغلق القائمة. لا تستخدم قائمة منسدلة صغيرة يصعب لمسها.

---

## 7. شاشة التحميل الافتتاحية

أنشئ Loading Screen تحمل الهوية ولا تبدو كSpinner عام. تظهر عند أول زيارة فقط أو أثناء تهيئة حرجة قصيرة. لا تؤخر العرض اصطناعيًا أكثر من اللازم. الحد الأعلى الموصى به 1.8 ثانية إذا كانت الموارد جاهزة، مع حد أمان يمنع بقاءها عالقة.

### التكوين البصري

- خلفية بلون Footer/Brand الداكن.
- نمط lattice خافت متحرك ببطء في الخلفية.
- الشعار نفسه في المنتصف، دون بدائل.
- أسفل الشعار خط تقدم رقيق أو قوس هندسي مستلهم من الشبكة.
- عبارة قصيرة مثل «مساحتك… بطريقتك» أو نص قابل للتعديل من Site Settings.
- دخول الشعار عبر fade + slight scale، ثم خروج الشاشة عبر reveal ناعم إلى الصفحة.

### السلوك

- أول زيارة في الجلسة: تشغيل التجربة.
- التنقل الداخلي: لا تعِد الشاشة الكاملة؛ استخدم progress indicator بسيطًا إن احتجت.
- `prefers-reduced-motion`: Fade سريع بلا تحريك هندسي.
- في حال JavaScript بطيء لا تمنع HTML الأساسي.
- أضف `aria-live` مناسبًا لعبارة التحميل، وتجنب تحديث النسبة كل جزء صغير.
- لا تعرض نسبة وهمية إن لم تكن مرتبطة بتحميل فعلي؛ يمكن استخدام progress indeterminate راقٍ.

---

## 8. الصفحة الرئيسية — ترتيب الأقسام

ترتيب الصفحة الرئيسية إلزامي:

1. Hero.
2. شريط/كتلة بحث سريعة عن السكن.
3. قسم القيمة أو «إقامة تناسبك».
4. الشقق المميزة/المتاحة.
5. كيف تعمل التجربة.
6. شريط الإحصائيات.
7. قسم تجربة السكن والخدمات.
8. قسم للملاك والمستثمرين مع CTA.
9. آراء العملاء.
10. الأسئلة الشائعة.
11. CTA ختامي.
12. Footer.

إذا كان المشروع الحالي يحتوي نصوصًا عربية تجارية معتمدة لهذه الأقسام، احتفظ بها. إن لم توجد استخدم النصوص المرجعية أدناه واجعلها من الـCMS.

### 8.1 Hero

Hero واسع وفخم، يبدأ مباشرة أسفل Header. استخدم Layout من عمودين على الديسكتوب: النص والبحث في اليمين، وتكوين صورة/صور سكنية في اليسار. على الموبايل يصبح عمودًا واحدًا والنص أولًا.

المحتوى المرجعي:

- Eyebrow: «إقامة مرنة، بتجربة أهدأ».
- H1: «مساحتك التي تشبهك، في المكان الذي تختاره».
- وصف: «شقق مختارة بعناية، جاهزة للسكن، وحجز واضح من البداية حتى الاستقرار.»
- CTA رئيسي: «تصفح الشقق».
- CTA ثانوي: «اعرض عقارك معنا».
- مؤشرات ثقة قصيرة ومدفوعة بالبيانات: «خيارات موثوقة»، «دعم مستمر»، «تفاصيل واضحة».

التكوين المرئي يمكن أن يكون بطاقة صورة كبيرة بنسبة 4:5 مع بطاقتين عائمتين: بطاقة موقع/حي، وبطاقة تقييم أو توفر. لا تكدّس أكثر من ثلاث طبقات. استخدم corner radius كبيرًا وظلًا هادئًا. حافظ على النص بعيدًا عن الصور المزدحمة.

### 8.2 البحث السريع

كتلة بحث واضحة يمكن أن تتداخل بصريًا قليلًا مع أسفل Hero على الديسكتوب. الحقول:

- المدينة.
- الحي (يتغير حسب المدينة).
- نوع الوحدة.
- المدة أو تاريخ الدخول إن كانت وظيفة الحجز تدعمه.
- زر «ابحث الآن».

استخدم Combobox accessible لا Select غير قابل للبحث عند كثرة الخيارات. القيم تأتي من API. عند الإرسال انتقل إلى `/apartments` مع query params قابلة للمشاركة. احتفظ بالقيم عند العودة. الموبايل يعرضها في عمود، أو زر «حدد خياراتك» يفتح Bottom Sheet منظمًا.

### 8.3 عرض القيمة

عنوان مثل «سكن مصمم ليواكب تفاصيل يومك»، ووصف قصير. اعرض 3 مزايا في بطاقات متوازنة:

- «جاهزة للسكن» — وحدات مؤثثة ومهيأة.
- «خيارات واضحة» — تفاصيل وصور وتكاليف قبل الطلب.
- «دعم يرافقك» — تواصل منظم خلال رحلة السكن.

المزايا Dynamic ويمكن تعديل ترتيبها وأيقوناتها ونشرها من لوحة التحكم.

### 8.4 الشقق المميزة

العنوان: «شقق مختارة لك» مع رابط «عرض جميع الشقق». اسحب مجموعة `featured` المنشورة فقط، مرتبة حسب `featuredRank` ثم تاريخ التحديث. اعرض 3 بطاقات على الديسكتوب، 2 على التابلت، وبطاقة كاملة أو horizontal snap محترم على الموبايل. لا تستخدم Carousel يخفي المحتوى على سطح المكتب إذا كانت شبكة أوضح.

كل بطاقة شقة تحتوي:

- الصورة الرئيسية مع نسبة ثابتة.
- Badge مثل «متاحة الآن»، «جديدة»، أو «الأكثر طلبًا» من البيانات.
- زر مفضلة إن كانت الميزة مفعّلة، مع حالة واضحة واسم accessible.
- اسم العقار/الوحدة.
- المدينة والحي مع أيقونة موقع.
- مختصر النوع والمساحة وعدد غرف النوم.
- السعر بصيغة عربية واضحة: «يبدأ من 4,500 ر.س / شهريًا» أو cadence من البيانات.
- تقييم وعدد المراجعات عند وجودهما فقط.
- CTA كامل البطاقة أو «عرض التفاصيل».

لا تعرض تقييم 0 أو نصًا فارغًا. إذا لم يتوفر السعر استخدم «تواصل لمعرفة السعر» من إعدادات المحتوى. لا تجعل البطاقة كلها زرًا يحتوي أزرارًا متداخلة؛ نفّذ Overlay Link بطريقة صحيحة.

### 8.5 كيف تعمل التجربة

قسم بثلاث أو أربع خطوات، مرقمة بصريًا:

1. «اختر وحدتك» — ابحث حسب الموقع والاحتياج.
2. «راجع التفاصيل» — شاهد الصور والمزايا والتكلفة.
3. «أرسل طلبك» — أدخل بيانات التواصل والمدة.
4. «ابدأ إقامتك» — يتولى الفريق تأكيد الخطوات.

على الديسكتوب خط رابط هندسي خافت بين الخطوات. على الموبايل Stack عمودي. النصوص والأيقونات والترتيب Dynamic.

### 8.6 الإحصائيات

شريط بلون Brand فاتح أو داكن حسب الهوية المستخرجة. الأرقام من API ولا تُكتب في المكون. أمثلة حقول:

- وحدات سكنية.
- أحياء مخدومة.
- نسبة رضا العملاء.
- سنوات الخبرة أو متوسط زمن الاستجابة.

استخدم أرقامًا حقيقية فقط. Animation العد يبدأ عند دخول viewport مرة واحدة، ويتوقف عند القيمة الصحيحة. مع reduced motion تُعرض القيمة مباشرة. دعم prefix/suffix مثل `+` و`%` و«ساعة».

### 8.7 تجربة السكن والخدمات

قسم Split Layout: صورة كبيرة أو Collage بسيط في جانب، ونص في الجانب الآخر. عنوان مثل «أكثر من مساحة للسكن»، ووصف يؤكد جودة التفاصيل. قائمة خدمات قابلة للإدارة مثل: أثاث وتجهيز، صيانة ودعم، مرونة في المدة، مواقع مختارة، تفاصيل دفع واضحة.

استخدم Check Icons هادئة، وزرًا إلى الشقق. يمكن عكس اتجاه الصورة والنص من لوحة التحكم عبر `mediaPosition` دون كسر RTL.

### 8.8 دعوة الملاك والمستثمرين

كتلة بارزة لكن منسجمة، بلون Footer أو Surface دافئ. العنوان: «حوّل عقارك إلى تجربة سكنية ناجحة». الوصف: «نعمل مع الملاك والمستثمرين لتجهيز وتشغيل وتسويق الوحدات بكفاءة ووضوح.» CTA: «اكتشف فرص الشراكة» يذهب إلى `/owners`.

أضف صورة معمارية أو Pattern، و3 نقاط ثقة قصيرة: تشغيل احترافي، وصول للمستأجرين، تقارير واضحة. كلها Dynamic.

### 8.9 آراء العملاء

العنوان: «تجارب تحكي عن منام». البيانات تأتي من Testimonials API. البطاقة تحتوي اقتباسًا، الاسم الأول أو الاسم المعروض، الصفة/نوع الإقامة اختياريًا، المدينة اختياريًا، Avatar اختياريًا، التقييم، والتاريخ الاختياري.

قواعد العرض:

- اعرض المعتمد والمنشور فقط.
- لا تنشر بيانات شخصية حساسة.
- اقتطع النص بصريًا عند الحاجة مع زر «اقرأ المزيد» accessible، لكن احتفظ بالمحتوى الكامل في DOM بطريقة سليمة أو Dialog.
- Carousel يدعم السحب، الأسهم، لوحة المفاتيح، وRTL، ولا يدور تلقائيًا بسرعة.
- أوقف autoplay عند hover/focus، والأفضل عدم تشغيله تلقائيًا.
- اعرض 3 بطاقات سطح مكتب، 2 تابلت، 1 موبايل.

### 8.10 FAQ

العنوان: «أسئلة شائعة». اعرض Accordion أحادي أو متعدد الفتح مع أزرار حقيقية و`aria-expanded` و`aria-controls`. الأسئلة تأتي من API حسب الصفحة/الفئة واللغة والترتيب. أمثلة موضوعات: طريقة الحجز، مدة الإقامة، ما يشمله السعر، سياسة الإلغاء، دعم الصيانة، عرض العقار.

لا تكتب الإجابات داخل المكون. اسمح لـCMS بمحتوى Rich Text محدود وآمن. ولّد FAQ schema فقط للأسئلة الظاهرة فعليًا والمتوافقة مع سياسات محركات البحث.

### 8.11 CTA الختامي

كتلة كبيرة قبل Footer بعنوان «جاهز تجد مساحتك؟»، وصف قصير، زر «تصفح الشقق»، وزر ثانوي للتواصل عند تفعيله. استخدم لونًا متباينًا وشكلًا بسيطًا من lattice. لا تكرر نص Hero حرفيًا.

---

## 9. صفحة تصفح الشقق

هذه صفحة تشغيلية وليست مجرد شبكة بطاقات. صممها لتستوعب آلاف الوحدات من دون إعادة بناء الواجهة.

### 9.1 مقدمة الصفحة

Breadcrumb: «الرئيسية / الشقق». H1: «اكتشف الشقق المتاحة». وصف قصير. يمكن إظهار عدد النتائج بعد التحميل بصياغة عربية سليمة. لا تدع تغيّر الرقم يسبب Layout Shift.

### 9.2 شريط البحث والفلاتر

الفلاتر المرجعية:

- نص بحث بالاسم أو الحي.
- المدينة.
- الحي.
- نوع الوحدة.
- نطاق السعر الأدنى/الأعلى.
- عدد غرف النوم.
- عدد دورات المياه عند أهمية ذلك.
- مفروشة/غير مفروشة أو مستوى التجهيز.
- المزايا (موقف، مصعد، إنترنت، دخول ذكي، تكييف، إلخ).
- متاحة من تاريخ.
- مدة الإقامة.
- ترتيب: الموصى بها، الأحدث، السعر تصاعدي، السعر تنازلي، الأعلى تقييمًا.

على الديسكتوب استخدم Sidebar Sticky أو شريط علوي + Drawer للفلاتر المتقدمة حسب المساحة. على الموبايل استخدم زر «الفلاتر» يفتح Bottom Sheet كامل الارتفاع تقريبًا، مع عدد الفلاتر المطبقة وزرين ثابتين: «عرض النتائج» و«إعادة التعيين».

زامن الفلاتر مع URL query params. استخدم debounce للبحث النصي. لا ترسل طلب API لكل حركة Range دون تهدئة. احذف القيم الافتراضية من URL. وفر Chips للفلاتر النشطة وإزالة كل فلتر منفردًا.

### 9.3 نتائج البحث

أعلى النتائج اعرض:

- العدد الإجمالي.
- Select الفرز.
- Toggle الشبكة/الخريطة فقط إذا كانت الخريطة منفذة فعليًا؛ لا تعرض زرًا غير عامل.

Grid: ثلاث بطاقات على الشاشات الواسعة، اثنتان على المتوسطة، واحدة على الموبايل. حافظ على تساوي تخطيط البطاقات حتى مع اختلاف طول الاسم. استخدم Pagination قابلة للفهرسة أو cursor-based load more. إن استخدمت Infinite Scroll فوفّر زر تحميل بديل وحافظ على موضع التمرير عند الرجوع من التفاصيل.

### 9.4 الحالات

- Loading: Skeleton مطابق لحجم البطاقات، 6 عناصر تقريبًا، بدون وميض.
- Empty: عنوان «لم نجد نتائج بهذه الخيارات»، شرح، زر مسح الفلاتر، واقتراحات مدن/شقق مميزة.
- Error: رسالة بشرية «تعذر تحميل الشقق الآن»، زر إعادة المحاولة، ولا تعرض stack trace.
- Partial error: إذا فشل عدد أو Facets وبقيت النتائج، لا تمسح المحتوى كله.
- Offline: وضح أن الاتصال غير متاح مع خيار المحاولة.

### 9.5 نموذج الاستجابة المرجعي

```ts
type ApartmentListItem = {
  id: string;
  slug: string;
  title: string;
  shortDescription?: string;
  city: { id: string; name: string; slug: string };
  district: { id: string; name: string; slug: string };
  propertyType: { id: string; name: string };
  coverImage: MediaAsset;
  bedrooms?: number;
  bathrooms?: number;
  areaSqm?: number;
  furnishing?: 'furnished' | 'semi_furnished' | 'unfurnished';
  price: Money | null;
  billingPeriod?: 'night' | 'week' | 'month' | 'year';
  rating?: number;
  reviewCount?: number;
  badges: Array<{ key: string; label: string; tone: string }>;
  availabilityStatus: 'available' | 'limited' | 'unavailable' | 'coming_soon';
  featured: boolean;
  updatedAt: string;
};
```

---

## 10. تفاصيل الشقة

التفاصيل لها مسار مستقل `/apartments/[slug]` لأغراض المشاركة وSEO. يمكن فتح Quick View من القائمة كنافذة كبيرة، لكن يجب أن يقود «عرض كل التفاصيل» إلى المسار الكامل.

### 10.1 رأس الصفحة والمعرض

- Breadcrumb.
- عنوان الوحدة.
- الموقع.
- التقييم وعدد المراجعات إن وجدا.
- أزرار مشاركة ومفضلة.
- معرض صور: صورة رئيسية كبيرة و4 صور مساعدة على الديسكتوب، صورة واحدة مع عداد على الموبايل.
- زر «عرض كل الصور» يفتح Lightbox accessible.

Lightbox يدعم لوحة المفاتيح، Swipe، Caption، زر إغلاق واضح، Focus Trap، وعدم تحميل الصور الأصلية كلها قبل الحاجة.

### 10.2 المحتوى الأساسي

قسم المعلومات يعرض:

- وصفًا مختصرًا ثم كاملًا.
- النوع، المساحة، الغرف، دورات المياه، الحد الأقصى للسكان.
- المزايا والخدمات في Grid.
- معلومات الموقع/الحي، وخريطة تقريبية إذا كانت الخصوصية تتطلب عدم إظهار الموقع الدقيق قبل الحجز.
- شروط الإقامة.
- المتاح وغير المتاح.
- سياسة الإلغاء المرتبطة بالعقار.
- FAQ خاص بالوحدة إن وجد.
- وحدات مشابهة في النهاية.

### 10.3 بطاقة السعر والطلب

على الديسكتوب بطاقة Sticky في الجانب، وعلى الموبايل Bottom Bar ثابت مختصر يفتح نموذج الطلب. تحتوي:

- السعر ووتيرة الفوترة.
- وديعة/رسوم خدمة/ضريبة إن كانت موجودة، بتفصيل واضح.
- تاريخ الدخول أو مدة الإقامة.
- زر «اطلب الحجز».
- رابط استفسار.
- تنبيه أن الإرسال لا يعني تأكيدًا فوريًا إذا كان هذا هو سير العمل.

لا تعرض سعرًا إجماليًا قبل اكتمال المدخلات إذا لم يمكن حسابه. لا تخفِ الرسوم حتى الخطوة الأخيرة.

### 10.4 نافذة طلب الحجز

Dialog على الديسكتوب وFull-screen Sheet على الموبايل. الحقول:

- الاسم الكامل.
- رقم الجوال مع مفتاح الدولة، الافتراضي +966 دون إجبار.
- البريد الإلكتروني اختياري أو مطلوب حسب إعدادات النظام.
- تاريخ/مدة السكن.
- عدد السكان.
- رسالة أو ملاحظات.
- موافقة صريحة على الشروط وسياسة الخصوصية مع روابط تفتح المحتوى القانوني.

استخدم validation عربيًا فوريًا لكن غير مزعج. لا تمسح البيانات عند فشل الشبكة. امنع الإرسال المكرر، واستخدم idempotency key. عند النجاح اعرض رقم الطلب والخطوة التالية، وأرسل حدث Analytics لا يحتوي PII.

---

## 11. صفحة الملاك والمستثمرين

مسار `/owners`. هذه الصفحة مستقلة بصريًا لكنها من الهوية نفسها. ترتيبها:

1. Hero للملاك والمستثمرين.
2. شريط ثقة/إحصائيات.
3. لماذا الشراكة مع منام.
4. نموذج التشغيل أو رحلة الشراكة.
5. الخدمات المقدمة.
6. أنواع الأصول المناسبة.
7. قصة نجاح أو شهادة شريك عند توفر بيانات حقيقية.
8. FAQ خاص بالملاك.
9. نموذج تقديم/CTA.
10. Footer.

### 11.1 Hero

H1 مرجعي: «نحوّل الأصول السكنية إلى تجارب ناجحة». الوصف: «شراكة متكاملة في التجهيز والتشغيل والتسويق، برؤية واضحة وتقارير تساعدك على اتخاذ القرار.» زران: «ابدأ شراكتك» و«تعرّف على آلية العمل». استخدم صورة معمارية عالية الجودة وتكوينًا راقيًا.

### 11.2 المزايا

بطاقات Dynamic:

- دراسة وتجهيز الأصل.
- إدارة التشغيل وتجربة الساكن.
- التسويق ورفع الإشغال.
- الصيانة والمتابعة.
- التقارير والشفافية.
- قابلية التوسع لمحفظة متعددة الوحدات.

### 11.3 رحلة الشراكة

1. استلام البيانات الأولية.
2. تقييم العقار والفرصة.
3. اقتراح نموذج التشغيل.
4. الاتفاق والتجهيز.
5. الإطلاق والتحسين المستمر.

اجعل الخطوات قابلة لإعادة الترتيب من لوحة التحكم.

### 11.4 نموذج الملاك

حقول مرحلية أو مجموعة منطقية:

- الاسم.
- الجوال.
- البريد.
- نوع مقدم الطلب: مالك/مستثمر/ممثل شركة.
- المدينة.
- نوع الأصل.
- عدد الوحدات.
- حالة الأصل: جاهز/قيد الإنشاء/يحتاج تجهيز.
- موقع تقريبي أو رابط خريطة اختياري.
- رسالة.
- رفع ملف/صور اختياري عبر Signed Upload، مع أنواع وأحجام محددة وفحص أمني.
- موافقة الخصوصية.

بعد الإرسال اعرض رقم Lead، وأرسل إشعارًا للخلفية. لا ترسل الملف عبر Payload JSON. خزّنه في Object Storage ثم اربط IDs. أضف rate limiting وCaptcha غير مزعجة عند الاشتباه، لا لكل مستخدم افتراضيًا.

---

## 12. سياسة الخصوصية والشروط والأحكام

روابط Footer «سياسة الخصوصية» و«الشروط والأحكام» تفتح محتوى قانونيًا فعليًا. على الديسكتوب يمكن أن يظهر Dialog عريض بارتفاع أقصى 80vh وتمرير داخلي، وعلى الموبايل Full-screen Sheet. وفي الوقت نفسه يجب أن يعمل `/privacy` و`/terms` كصفحتين مباشرتين قابلتين للمشاركة والفهرسة.

### متطلبات Dialog القانوني

- عنوان ثابت في الأعلى.
- تاريخ آخر تحديث من CMS.
- Table of contents اختياري للمحتوى الطويل.
- محتوى Rich Text منسق وآمن.
- زر إغلاق واضح.
- Focus Trap وعودة Focus.
- Escape للإغلاق.
- عدم إغلاقه عرضيًا عند تحديد النص؛ Overlay click يمكن تعطيله للمحتوى القانوني.
- لا تستخدم HTML خامًا غير معقم.
- روابط داخلية وخارجية واضحة.
- دعم الطباعة.

النص القانوني نفسه لا يُخترع كاستشارة قانونية. أنشئ Content Placeholders منظمة بعناوين نموذجية: البيانات التي نجمعها، كيفية الاستخدام، الأساس/الموافقة، المشاركة، الحفظ، الأمان، حقوق المستخدم، ملفات الارتباط، الأطفال، التعديلات، التواصل. يجب أن يراجعه مختص قانوني قبل الإنتاج. الأمر نفسه للشروط: الأهلية، الحساب، الطلب والحجز، الأسعار، الدفع، الإلغاء، الاستخدام المقبول، مسؤوليات الأطراف، الملكية الفكرية، الإنهاء، حدود المسؤولية، النظام الحاكم، التواصل.

---

## 13. Footer

Footer عنصر هوية رئيسي، واستخدم اللون الموجود في المشروع الحالي حرفيًا بعد تحويله إلى Token. إذا تعذر، استخدم Brand 900. تكوينه:

- الصف/العمود الأول: الشعار نفسه `/dark-logo.png`. إذا كان الشعار الداكن غير مقروء على الخلفية الداكنة، لا تستبدله بشعار آخر؛ ضع خلفه Surface فاتحًا صغيرًا أو استخدم CSS treatment غير مدمّر بعد التحقق البصري.
- وصف قصير للمنصة.
- روابط التواصل الاجتماعي من Site Settings فقط.
- مجموعة «استكشف»: الرئيسية، الشقق، للملاك والمستثمرين.
- مجموعة «الدعم»: الأسئلة الشائعة، تواصل معنا، قنوات الدعم.
- مجموعة «قانوني»: الخصوصية، الشروط.
- بيانات الاتصال وساعات العمل إن كانت مفعّلة.
- سطر حقوق النشر بسنة ديناميكية واسم المنصة.

على الموبايل يتحول إلى Stack أو Accordion للروابط، لكن الشعار والوصف يبقيان ظاهرين. التباين WCAG AA على الأقل. حالات Hover/Focus واضحة. لا تضع روابط وهمية `#`؛ أخفِ العنصر حتى يُضبط.

---

## 14. هندسة المكونات

اعتمد Atomic/Feature-oriented architecture دون إفراط. التقسيم المرجعي:

```text
src/
├── app/ or routes/
├── components/
│   ├── ui/              # Button, Input, Dialog, Drawer, Badge, Skeleton...
│   ├── layout/          # Header, Footer, Container, Section...
│   └── shared/          # Logo, RichText, ErrorState, EmptyState...
├── features/
│   ├── apartments/
│   │   ├── components/
│   │   ├── api/
│   │   ├── hooks/
│   │   ├── schemas/
│   │   └── types/
│   ├── booking/
│   ├── owners/
│   ├── testimonials/
│   ├── faq/
│   ├── legal/
│   └── site-content/
├── lib/
│   ├── api-client/
│   ├── auth/
│   ├── analytics/
│   ├── formatting/
│   └── validation/
├── styles/
├── config/
└── test/
```

قواعد المكونات:

- `ApartmentCard` لا يجلب البيانات بنفسه؛ يستقبل نوعًا واضحًا.
- `ApartmentGrid` يدير التخطيط فقط.
- `ApartmentFilters` يعكس Filter State إلى URL.
- `SectionHeader` يقبل eyebrow/title/description/action/alignment.
- `ContentSection` يقبل variants محدودة، لا عشرات props المتعارضة.
- `Dialog` و`Drawer` primitives موحدة للوصول.
- `Button` يدعم `primary`, `secondary`, `ghost`, `danger` مع أحجام ثابتة وحالة loading.
- لا تكرر HTML للنماذج بين Modal والصفحة.
- Server Components للمحتوى والفهرسة إن كان Next.js، وClient Components فقط للتفاعل.
- ضع حدودًا واضحة بين Domain Types وAPI DTOs وView Models.

---

## 15. المحتوى الديناميكي ولوحة التحكم

الموقع العام يجب أن يقرأ من طبقة Content/Domain APIs. لوحة التحكم ليست بالضرورة ضمن نفس التسليم البصري، لكن يجب أن تكون البنية جاهزة لها.

### الكيانات القابلة للإدارة

1. Apartments/Properties/Units.
2. المدن والأحياء.
3. أنواع العقارات والمزايا.
4. الأسعار والفترات والرسوم.
5. التوفر.
6. الصور والوسائط وترتيبها وalt text.
7. Testimonials وحالة اعتمادها.
8. FAQ والفئات والصفحات المرتبطة.
9. Page Sections ومحتواها وترتيبها وحالة نشرها.
10. الإحصائيات.
11. الشركاء/الشعارات عند وجودها.
12. النصوص القانونية وإصداراتها.
13. Site Settings ووسائل التواصل وFooter وSEO defaults.
14. Leads الخاصة بالحجز والملاك.
15. Redirects وSlugs القديمة.

### دورة النشر

كل محتوى قابل للتحرير يحتاج حالات `draft`, `scheduled`, `published`, `archived`. دعم `publishedAt`, `unpublishedAt`, `createdBy`, `updatedBy`, وrevision history للمحتوى الحساس. لا يرى الموقع العام المسودات إلا Preview Token آمن. عند النشر امسح/أعد Validation للكاش المتأثر فقط.

### ترتيب العناصر

استخدم حقول `sortOrder` أو rank ثابت، ولا تعتمد على تاريخ الإنشاء كترتيب دائم. يجب أن تتمكن لوحة التحكم من السحب وإعادة الترتيب مع تحديث Transactional يمنع التكرار.

### الترجمة

حتى لو كانت العربية الوحيدة الآن، صمم المحتوى ليقبل locale بدون تعقيد الواجهة. استخدم جداول translations أو JSONB منظم حسب قرار الـBackend. لا تضع الإنجليزية في الواجهة ولا تعرض Locale switcher حاليًا.

---

## 16. نموذج قاعدة البيانات المتوقع

استخدم PostgreSQL مرجعيًا. يمكن تكييفه مع ORM المستخدم. استخدم UUIDs، timestamps بتوقيت UTC، soft delete عند الحاجة، وفهارس حقيقية.

### الجداول الأساسية

```text
users
roles
permissions
user_roles
audit_logs

cities
districts
property_types
properties
units
unit_media
amenities
unit_amenities
pricing_plans
availability_periods
unit_badges

testimonials
faq_categories
faqs
page_definitions
page_sections
site_settings
navigation_items
legal_documents
legal_document_versions
media_assets

booking_leads
owner_leads
lead_attachments
lead_status_history
contact_requests

redirects
webhook_deliveries
outbox_events
```

### properties

يمثل المبنى/الأصل:

```text
id uuid pk
owner_id uuid nullable/internal
name varchar
slug varchar unique
description text
city_id uuid fk
district_id uuid fk
address_line text nullable
latitude numeric nullable
longitude numeric nullable
location_precision enum(exact, approximate, hidden)
status enum(draft, active, inactive, archived)
created_at timestamptz
updated_at timestamptz
deleted_at timestamptz nullable
```

### units

```text
id uuid pk
property_id uuid fk
property_type_id uuid fk
title varchar
slug varchar unique
short_description text
description richtext/jsonb
bedrooms smallint nullable
bathrooms numeric nullable
area_sqm numeric nullable
max_occupancy smallint nullable
furnishing enum
floor_number varchar nullable
availability_status enum
featured boolean default false
featured_rank integer nullable
publication_status enum
published_at timestamptz nullable
seo_title varchar nullable
seo_description varchar nullable
created_at/updated_at/deleted_at
```

### media_assets وunit_media

افصل الأصل الرقمي عن علاقته بالوحدة. الحقول: storage key، mime type، width، height، size، blur hash، focal point، alt text، copyright، processing status. علاقة الوحدة تضيف `sort_order`, `is_cover`, `caption`. امنع أكثر من Cover فعّال عبر constraint أو transaction.

### pricing_plans

```text
id, unit_id
currency char(3) default SAR
amount_minor bigint
billing_period enum(night, week, month, year)
min_stay integer nullable
deposit_minor bigint nullable
service_fee_minor bigint nullable
tax_included boolean
valid_from/valid_to nullable
is_active boolean
```

لا تستخدم float للأموال. خزّن halalas كعدد صحيح أو decimal مضبوط. نسّق العملة في الواجهة عبر `Intl.NumberFormat('ar-SA', { currency: 'SAR' })`.

### testimonials

الحقول: display_name، role/label، quote، avatar media id، rating، city، source، consent recorded، moderation status، publication status، featured، sort order، published at. لا تنشر testimonial قبل الموافقة.

### faqs

question، answer richtext، category id، page key، sort order، publication status، locale، schema eligibility.

### page_sections

`page_key`, `section_key`, `section_type`, `content_json`, `sort_order`, `is_enabled`, `publication_status`, `version`. تحقق من `content_json` باستخدام Schema حسب `section_type`، ولا تسمح بأي JSON عشوائي يكسر الواجهة.

### leads

Booking Lead يرتبط بـunit، ويخزن snapshot مرجعيًا للسعر/الطلب عند الإرسال، لا بيانات البطاقة البنكية. Owner Lead يخزن تفاصيل الأصل. كلاهما له status، source، UTM، consent version، assignee اختياري، timestamps، وhistory. شفّر الحقول الحساسة حسب متطلبات المنظومة، وطبّق retention policy.

### الفهارس والقيود

- unique index على slugs غير المحذوفة.
- index على publication_status + published_at.
- index على city/district/type/availability/featured.
- فهرس بحث نصي للعناوين والوصف والأحياء إذا كان البحث في PostgreSQL.
- قيود rating بين 1 و5، وأسعار غير سالبة، وعدد غرف غير سالب.
- foreign keys بسياسات حذف واعية؛ لا cascade حذف Leads.
- optimistic locking أو version للحالات التحريرية.

---

## 17. عقود API

استخدم REST JSON أو GraphQL وفق المشروع، لكن حافظ على العقود التالية. النسخة المرجعية REST تحت `/api/v1`.

### عامة

```text
GET /api/v1/site
GET /api/v1/pages/:pageKey
GET /api/v1/navigation
GET /api/v1/legal/:type
GET /api/v1/faqs?page=home&category=...
GET /api/v1/testimonials?featured=true&limit=...
GET /api/v1/locations/cities
GET /api/v1/locations/cities/:cityId/districts
```

### الشقق

```text
GET /api/v1/apartments
GET /api/v1/apartments/:slug
GET /api/v1/apartments/:slug/similar
GET /api/v1/apartments/facets
```

Query params موحدة: `q`, `city`, `district`, `type`, `priceMin`, `priceMax`, `bedrooms`, `amenities[]`, `availableFrom`, `duration`, `sort`, `page`, `pageSize`, أو `cursor`.

استجابة القائمة:

```json
{
  "data": [],
  "meta": {
    "page": 1,
    "pageSize": 12,
    "total": 0,
    "totalPages": 0
  },
  "facets": {
    "cities": [],
    "districts": [],
    "propertyTypes": [],
    "bedrooms": [],
    "amenities": [],
    "price": { "min": 0, "max": 0 }
  }
}
```

### الطلبات

```text
POST /api/v1/booking-leads
POST /api/v1/owner-leads
POST /api/v1/contact-requests
POST /api/v1/uploads/sign
```

كل POST يقبل `Idempotency-Key`. الرد 201 يحتوي ID عام/مرجعًا وحالة وnext step. أخطاء التحقق 422 بشكل حقول منظم:

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "تحقق من البيانات المدخلة",
    "fields": {
      "phone": ["أدخل رقم جوال صحيحًا"]
    },
    "requestId": "..."
  }
}
```

لا ترسل تفاصيل قاعدة البيانات. استخدم 409 للتعارض، 429 للحد، 404 لغير المنشور، و500 برسالة عامة. سجّل request ID.

### Admin API المتوقع

CRUD مع pagination/filtering لـapartments, testimonials, FAQs, page sections, legal docs, settings, leads. استخدم RBAC، audit logs، optimistic concurrency عبر version/ETag، bulk reorder، preview، publish/unpublish. لا تجعل الواجهة العامة تستدعي Admin endpoints.

### الكاش

- Site settings/navigation/legal: كاش أطول مع revalidation عند النشر.
- قوائم الشقق: كاش قصير حسب الفلاتر، أو CDN مع tags.
- تفاصيل الشقة: revalidation حسب unit ID.
- POST: بلا كاش.
- لا تعرض بيانات قديمة بعد إلغاء نشر وحدة؛ webhook أو tag invalidation إلزامي.

---

## 18. إدارة الحالة والبيانات

- حالة الفلاتر في URL.
- حالة النماذج محلية عبر React Hook Form أو مكافئ.
- Server state عبر Query library أو server fetching؛ لا تنسخها إلى global store.
- UI state العامة المحدودة مثل Drawer/Toast فقط في context/store صغير.
- استخدم abort signal عند تغيير البحث بسرعة.
- حدد retry: GET مرتان مع backoff، POST لا يعاد تلقائيًا إلا مع idempotency وضمان.
- اعرض بيانات cache أثناء التحديث مع مؤشر subtle.
- تحقق من كل API response باستخدام Zod أو Schema على الحدود عند الحاجة.

أنشئ Mock Repository مطابقًا للعقد عند غياب Backend. التبديل بين mock وreal عبر environment config، وليس تعديلات في المكونات. اجعل fixtures عربية واقعية ومنفصلة، مع 12 شقة على الأقل لاختبار pagination، حالات أسعار مختلفة، صور مفقودة، تقييمات غائبة، ونصوص طويلة.

---

## 19. Responsive بالتفصيل

اختبر على 320، 375، 390، 430، 768، 1024، 1280، 1440، و1920px.

### الموبايل

- لا يوجد تمرير أفقي.
- العناوين لا تخرج عن الشاشة.
- الأزرار المهمة كاملة العرض عند الحاجة.
- Touch targets 44px على الأقل.
- البطاقات بعمود واحد.
- الفلاتر Bottom Sheet.
- Booking CTA ثابت مع safe-area padding.
- الجداول تتحول إلى بطاقات أو scroll region معلّم.
- الصور بارتفاع متزن لا يستهلك الشاشة كاملة.
- Dialogs القانونية Full-screen.

### التابلت

- Grid عمودان للشقق.
- Header قد يستخدم Drawer إذا لم تتسع الروابط.
- Hero يحتفظ بعمودين فقط إن لم يضغط النص؛ وإلا stack.
- المسافات بين الديسكتوب والموبايل.

### الديسكتوب

- Container لا يتمدد بلا حدود.
- بطاقات الشقق 3 أعمدة، وربما 4 فقط في 1600+ إذا بقي عرض البطاقة 280px على الأقل.
- Filters sidebar sticky دون تجاوز viewport.
- Booking card sticky مع نهاية محددة ضمن القسم.

استخدم CSS logical properties مثل `margin-inline`, `padding-inline`, `inset-inline-start`. لا تستخدم left/right إلا للزخرفة مع فهم RTL.

---

## 20. الحركة والتفاعل

نظام الحركة:

- micro: 120–180ms.
- component: 180–260ms.
- section reveal: 350–550ms.
- easing موحد مثل `cubic-bezier(.2,.8,.2,1)`.

التفاعلات:

- الأزرار: تغير لون/ظل بسيط وtranslateY أقصى 1–2px.
- البطاقات: رفع خفيف، تكبير صورة 1.02، وحد أو ظل أوضح.
- الروابط: underline/indicator ينمو باتجاه منطقي.
- الأقسام: fade + translate 12–20px مرة واحدة.
- Accordion: height/opacity دون قفز.
- Dialog: overlay fade، panel scale بسيط أو slide منطقي.
- Toast: دخول وخروج هادئ.

لا تحرك كل عنصر منفردًا بفارق كبير. لا تستخدم parallax ثقيلًا. لا تجعل Hover شرطًا لفهم المعلومة. اختبر reduced motion بإلغاء transform والحركة المتكررة.

---

## 21. الوصول Accessibility

الحد الأدنى WCAG 2.2 AA:

- `lang="ar"`, `dir="rtl"`.
- Skip link إلى المحتوى.
- هيكل عناوين منطقي H1 واحد لكل صفحة.
- Landmarks: header/nav/main/footer.
- جميع الوظائف بلوحة المفاتيح.
- Focus ring واضح غير محذوف.
- تباين النص 4.5:1، والكبير 3:1، والمكونات 3:1.
- Labels مرئية للنماذج؛ placeholder ليس label.
- أخطاء مرتبطة بـ`aria-describedby` وملخص عند الإرسال.
- إعلان نجاح/فشل عبر live region متزن.
- Modal focus trap وعودة focus.
- Carousel controls بأسماء واضحة وحالة الشريحة.
- لا تعتمد على اللون وحده.
- Alt عربي مدروس.
- Tooltip ليس المصدر الوحيد للمعلومة.
- عند تعطيل زر، اشرح السبب أو اسمح بالنقر وإظهار validation بحسب السياق.
- Zoom 200% وText spacing لا يكسر التصميم.
- دعم VoiceOver/NVDA قدر الإمكان.

اختبر آليًا بـaxe، لكن لا تعتبر نجاحه بديلًا عن اختبار لوحة المفاتيح وقارئ الشاشة.

---

## 22. SEO والبيانات المنظمة

- SSR/SSG للمحتوى العام.
- metadata عربية فريدة لكل صفحة.
- canonical URLs.
- Open Graph وTwitter cards بصور مناسبة.
- Sitemap ديناميكي يشمل الشقق المنشورة فقط.
- robots.txt.
- BreadcrumbList schema.
- Organization/LocalBusiness حسب صحة النشاط والبيانات.
- FAQPage للأسئلة الظاهرة والمؤهلة فقط.
- Product/Apartment/Accommodation schema بما يتوافق مع نوع العرض الحقيقي، دون Rating وهمي.
- Slugs مستقرة، و301 Redirect عند تغييرها.
- noindex لنتائج فلاتر منخفضة القيمة حسب استراتيجية SEO، مع canonical مناسب.
- لا تفهرس preview أو admin أو صفحات النجاح الخاصة.

عنوان التفاصيل يتضمن اسم الوحدة والحي/المدينة طبيعيًا. الوصف meta من محتوى معتمد، بطول مناسب. لا تحشو الكلمات المفتاحية.

---

## 23. الأداء

الأهداف على موبايل متوسط واتصال 4G:

- LCP أقل من 2.5s.
- INP أقل من 200ms.
- CLS أقل من 0.1.
- Lighthouse Performance/Accessibility/Best Practices/SEO هدف 90+ مع تفسير أي استثناء.

الإجراءات:

- تحسين الصور إلى AVIF/WebP مع srcset/sizes.
- preload للخط الأساسي وHero فقط.
- subset عربي للخط إن أمكن قانونيًا وتقنيًا.
- lazy load للمقاطع تحت Fold.
- code splitting للخرائط وLightbox والنوافذ الثقيلة.
- لا تشحن مكتبة خرائط أو Slider ضخمة قبل الحاجة.
- حجز أبعاد الصور والأيقونات.
- تقليل Client JS باستخدام Server Components عند توفرها.
- tree shaking ومنع imports واسعة.
- cache headers صحيحة للأصول hashed.
- تحليل bundle وإزالة التكرار.
- لا تستخدم فيديو Hero تلقائيًا إلا إذا كان موجودًا ومتطلبًا ومُحسنًا؛ الأفضل صورة.

---

## 24. الأمان والخصوصية

- تحقق من المدخلات على العميل والخادم، والخادم هو المرجع.
- sanitize للـRich Text مع allowlist.
- حماية CSRF حسب أسلوب المصادقة.
- CORS مقيد.
- rate limiting على النماذج.
- منع XSS وSQL injection عبر ORM/queries parameterized.
- Content Security Policy قابلة للتشغيل.
- Secure headers: HSTS, frame-ancestors, referrer policy, permissions policy.
- Signed URLs للرفع، تحقق MIME فعليًا، فحص malware، حد الحجم والعدد.
- لا تسجل أرقام الجوال أو الرسائل كاملة في logs.
- لا ترسل PII إلى Analytics.
- خزّن موافقة الشروط برقم إصدار وتاريخ.
- طبق retention/deletion للـLeads وفق السياسة المعتمدة.
- الأسرار في env/server فقط، ولا تستخدم `NEXT_PUBLIC_` للأسرار.
- Error monitoring ينقح البيانات الحساسة.

---

## 25. Analytics والمراقبة

أنشئ طبقة Analytics محايدة provider-agnostic. الأحداث المقترحة:

```text
page_view
hero_cta_clicked
apartment_search_submitted
apartment_filter_applied
apartment_card_opened
apartment_gallery_opened
booking_form_started
booking_form_submitted
booking_form_succeeded
booking_form_failed
owner_form_started
owner_form_succeeded
legal_document_opened
faq_expanded
```

أرسل IDs داخلية/تصنيفات فقط، دون أسماء أو جوالات أو بريد أو رسائل. دعم consent mode إن كانت أدوات غير ضرورية. راقب الأخطاء، Core Web Vitals، معدل نجاح APIs، ومدة الاستجابة. أضف health checks وstructured logs في الخلفية.

---

## 26. الاختبارات

### Unit

- تنسيق السعر والعملات.
- تحويل الفلاتر من/إلى URL.
- validation للنماذج.
- اختيار badges وحالات السعر.
- schemas والـmappers.

### Component

- ApartmentCard مع بيانات كاملة وناقصة.
- FAQ keyboard behavior.
- Dialog focus trap/return.
- Filters add/remove/reset.
- Form validation and server errors.
- Header mobile navigation.

### Integration

- قائمة الشقق مع mock API وفلاتر وpagination.
- تفاصيل الوحدة وحالات 404/unavailable.
- إرسال Booking Lead مع idempotency.
- فتح privacy/terms من Footer ثم الرجوع.

### E2E

1. الدخول، اكتمال Loading، تصفح الصفحة.
2. البحث عن مدينة والانتقال للنتائج.
3. تطبيق فلاتر ومشاركة URL وإعادة تحميله.
4. فتح شقة، الصور، ثم إرسال طلب صحيح.
5. اختبار أخطاء الحقول وفشل الشبكة ثم retry دون فقدان البيانات.
6. فتح صفحة الملاك وإرسال Lead.
7. فتح الخصوصية والشروط من Footer بالموبايل والديسكتوب.
8. التنقل كاملًا بلوحة المفاتيح.

### Visual Regression

Snapshots على المقاسات الأساسية، خاصة Header، Hero، Apartment Card، Footer، Modals، loading/empty/error، والنص العربي الطويل. لا تعتمد على snapshot وحيد؛ راجع الفروق بصريًا.

---

## 27. حالات الحواف الضرورية

تعامل صراحة مع:

- عنوان شقة طويل جدًا.
- حي أو مدينة باسم طويل.
- غياب صورة أو وجود صورة واحدة.
- غياب تقييم أو سعر.
- شقة غير متاحة أثناء فتح المستخدم لها.
- نتائج أكثر من آلاف العناصر.
- API بطيء أو يرجع 500.
- FAQ بإجابة طويلة وروابط وقوائم.
- نص قانوني طويل جدًا.
- مستخدم يرسل النموذج مرتين.
- رقم دولي غير سعودي.
- انقطاع الاتصال بعد ملء النموذج.
- تعطيل JavaScript جزئيًا للمحتوى الأساسي.
- روابط اجتماعية غير مضبوطة.
- تغيير slug.
- محتوى CMS غير صالح أو section type غير معروف: تجاهله بأمان وسجّل الخطأ، ولا تكسر الصفحة.
- توقيت نشر مجدول مع UTC وعرض بتوقيت مناسب في لوحة التحكم.

---

## 28. معايير جودة الكود

- TypeScript strict، لا تستخدم `any` دون تبرير.
- ESLint/formatter متوافقان مع المشروع.
- مكونات صغيرة ذات مسؤولية واضحة، لكن لا تفتت كل سطر.
- أسماء إنجليزية واضحة للملفات والأنواع، ومحتوى عربي عبر CMS/i18n.
- لا تكرر قيم الألوان والمسافات.
- لا تستخدم magic numbers للحالات المهمة.
- لا تضع fetch داخل عناصر العرض البسيطة.
- لا تبتلع الأخطاء silently.
- Error Boundaries على مستوى route/feature.
- تعليقات تشرح «لماذا» فقط.
- لا تترك dead code أو console logs أو TODOs حرجة.
- README يشرح التشغيل، env، mocks، الاختبارات، build، وربط الـAPI.
- `.env.example` بأسماء المتغيرات فقط.

---

## 29. خطة التنفيذ المطلوبة من النموذج

نفّذ على مراحل، لكن لا تتوقف قبل إكمال المراحل الممكنة:

### المرحلة 1: التدقيق والأساس

- اكتشاف المشروع.
- تثبيت Tokens والخط وRTL والخلفية.
- توحيد primitives: Button/Input/Dialog/Container.
- إصلاح Header/Footer/Layout.

### المرحلة 2: طبقة البيانات

- Types وSchemas وAPI client.
- Mock repository وfixtures.
- Query keys/caching/error mapping.
- Site settings/content contracts.

### المرحلة 3: الصفحة الرئيسية

- Loading screen.
- Hero والبحث.
- الأقسام بالترتيب.
- الشقق والتعليقات وFAQ من repository.
- حالات responsive والحركة.

### المرحلة 4: الشقق

- listing + URL filters + states.
- التفاصيل والمعرض.
- similar units.
- Booking flow.

### المرحلة 5: الملاك والقانوني

- owners page + lead form.
- privacy/terms routes + dialogs.
- Footer integration.

### المرحلة 6: الجودة

- Accessibility.
- SEO/schema/sitemap.
- image/font optimization.
- tests.
- visual verification.
- production build.

بعد كل مرحلة شغّل lint/typecheck/tests المناسبة. لا تؤجل كل التحقق إلى النهاية.

---

## 30. قائمة القبول النهائية

لا تعتبر المهمة مكتملة حتى تتحقق البنود التالية:

### الهوية

- [ ] IBM Plex Sans Arabic مطبق فعليًا في كل الواجهة.
- [ ] Light Mode فقط ولا يوجد Dark Mode.
- [ ] `/public/dark-logo.png` هو مصدر كل الشعارات.
- [ ] lattice pattern يغطي الموقع بنسبة 20% دون تخفيض opacity المحتوى.
- [ ] Footer يستخدم اللون الفعلي للهوية أو fallback المحدد.
- [ ] الألوان والمسافات وأنصاف الأقطار موحدة عبر Tokens.

### الصفحات

- [ ] الصفحة الرئيسية كاملة وبالترتيب المحدد.
- [ ] Browse Apartments تعمل بالفلاتر والفرز والـURL.
- [ ] تفاصيل الشقة قابلة للمشاركة والفهرسة.
- [ ] Booking flow يعمل مع نجاح وفشل وتحميل.
- [ ] صفحة الملاك كاملة ونموذجها يعمل.
- [ ] الخصوصية والشروط تعملان كرابطين مباشرين وكواجهة منبثقة.
- [ ] Loading Screen لا تعلق ولا تتكرر في كل تنقل.

### الديناميكية

- [ ] لا توجد شقق hardcoded داخل المكونات.
- [ ] Testimonials من data layer.
- [ ] FAQs من data layer.
- [ ] الإحصائيات والأقسام وFooter وSEO قابلة للإدارة.
- [ ] إضافة عنصر من Dashboard/API تنعكس دون تعديل الكود.
- [ ] النشر وإلغاء النشر يعيدان Validation للكاش.

### الجودة

- [ ] Responsive من 320 إلى 1920 دون overflow.
- [ ] Keyboard navigation كامل.
- [ ] Focus/labels/contrast/modals سليمة.
- [ ] reduced motion مدعوم.
- [ ] loading/empty/error/offline منفذة.
- [ ] الصور محسنة بلا CLS.
- [ ] لا أخطاء TypeScript أو lint.
- [ ] production build ينجح.
- [ ] الاختبارات الحرجة تنجح.
- [ ] لا أسرار أو PII في العميل/logs/analytics.

---

## 31. صيغة التسليم من نموذج البرمجة

في نهاية التنفيذ، قدّم تقريرًا مختصرًا ودقيقًا يتضمن:

1. ما تم إنجازه فعليًا.
2. الملفات/المناطق الرئيسية التي تغيرت.
3. طريقة التشغيل.
4. متغيرات البيئة المطلوبة.
5. نتيجة typecheck/lint/tests/build.
6. ما يستخدم Mock Data وكيفية توصيل Backend الحقيقي.
7. أي متطلب تعذر تنفيذه مع سبب محدد، لا عبارات عامة.
8. لقطات أو تحقق بصري للمقاسات الرئيسية إن كانت البيئة تسمح.

لا تقل «Production Ready» إذا لم ينجح build أو كانت النماذج وهمية دون توضيح. لا تسلّم صفحات شكلية بأزرار غير عاملة. لا تكتفِ بوصف الكود؛ عدّل المشروع وشغّل التحقق.

---

## 32. توجيهات أخيرة حاسمة

المطلوب تجربة متكاملة تبدو وكأن فريق Product محترف صممها ونفذها، لا تجميع Sections جاهزة. حافظ على الهدوء البصري، وضوح التسلسل، جودة العربية، وسهولة الوصول إلى الشقق أو عرض العقار. اجعل المستخدم يفهم خلال ثوانٍ ما تقدمه «منام»، ثم يجد طريقه إلى الإجراء المناسب دون ارتباك.

عند وجود تعارض بين جمال بصري وحسن استخدام، حافظ على حسن الاستخدام مع صياغة جمالية. عند وجود تعارض بين بيانات حقيقية وواجهة ممتلئة، اعرض حالة فارغة أنيقة ولا تخترع أرقامًا أو تقييمات. عند غياب Backend، أنشئ Contract وMock Adapter كاملين بحيث يكون استبدال المصدر لاحقًا تغييرًا في طبقة واحدة. وعند وجود جزء صالح في المشروع، حسّنه بدل هدمه.

ابدأ الآن بفحص المشروع، ثم نفّذ جميع المراحل، ثم تحقق بصريًا ووظيفيًا، ثم سلّم التقرير النهائي فقط بعد نجاح البناء والاختبارات الأساسية.

---

# الملحق التنفيذي الإلزامي: المحتوى الحرفي والترتيب المرجعي النهائي

> هذا الملحق أعلى أولوية من أي نص عام سابق في الوثيقة. عند وجود تعارض في ترتيب الأقسام أو الصياغة أو شكل صفحة، طبّق هذا الملحق. المطلوب إعادة إنتاج هذه البنية والمحتويات دون الإشارة إلى أي مرجع بصري خارجي، ودون انتظار ملفات إضافية من المستخدم.

## 33. التصحيح الأساسي لنطاق المنتج

المنصة تجمع مسارين تجاريين متكاملين:

1. مسار النزيل: تصفح وحدات فندقية راقية، الاطلاع على التفاصيل، ثم تقديم طلب حجز ذكي.
2. مسار المالك أو المستثمر: تسليم العقار لمنام كي تديره وتشغله فندقيًا، مع هدف زيادة العائد وتقليل عبء التشغيل.

الصفحة الرئيسية ليست صفحة حجز تقليدية فقط؛ رسالتها الكبرى موجهة للمالك، ثم تعرض خبرة التشغيل والوحدات المتاحة للنزيل. لذلك لا تستبدل العنوان الرئيسي برسالة عامة مثل «ابحث عن بيتك». التزم بالرسالة التجارية المحددة أدناه.

الوضع فاتح فقط. احذف زر القمر وأي منطق أو Theme مظلم من النسخة النهائية. ويمكن إبقاء زر اللغة «English» فقط إذا كان التحويل الإنجليزي منفذًا كاملًا؛ وإلا أخفه حتى لا يكون زرًا وهميًا. النسخة العربية هي الافتراضية.

## 34. Header المرجعي

Header أبيض صافٍ، ارتفاعه قرابة 112–128px على سطح المكتب، مع حد سفلي رقيق جدًا. المحتوى داخل container بعرض يقارب 1360px. الترتيب RTL:

- الشعار في أقصى اليمين من `/dark-logo.png`، بعرض بصري يقارب 52–68px وبدون اسم مكتوب بجانبه.
- في المنتصف ثلاثة روابط فقط وبالترتيب: «الرئيسية»، «تصفح الشقق»، «للملاك والمستثمرين».
- الرابط النشط أخضر داكن وبوزن 600 وتحته خط أخضر قصير.
- في أقصى اليسار زر اللغة «English» مع أيقونة الكرة الأرضية عند تفعيل الإنجليزية.
- لا يوجد زر Dark Mode.

Header ثابت أو Sticky. عند التمرير يحتفظ بالخلفية البيضاء والحد الرقيق مع ظل شبه غير ملحوظ. على الموبايل استخدم شعارًا وقائمة Drawer، مع إبقاء ترتيب الروابط نفسه.

## 35. الصفحة الرئيسية: الترتيب الحرفي

الترتيب الإلزامي:

1. Hero الملاك والعائد.
2. «في منام» مع الخدمات التشغيلية والإحصائيات.
3. «تصفح باقة مختارة من أرقى وحداتنا السكنية».
4. «تقييمات النزلاء وتجاربهم».
5. «الأسئلة الشائعة» بعمودين وفئتين.
6. «منظومة شركاء النجاح والمنصات المعتمدة».
7. Footer الكامل.

لا تضف أقسامًا عامة بين هذه الأقسام مثل «كيف تعمل» أو CTA ختامي أو بحث سريع إلا إذا كانت موجودة أصلًا في المشروع ومعتمدة صراحة. المحافظة على هذا التسلسل أهم من ملء الصفحة بأقسام إضافية.

## 36. Hero الصفحة الرئيسية

Hero مركزي، نصي بالكامل، بلا صورة عقار كبيرة. يغطي معظم الارتفاع الأول بعد Header، مع lattice pattern خلفه. استخدم Eyebrow صغيرًا اختياريًا قابلًا للتحرير، ثم:

### العنوان حرفيًا

```text
أعد تعريف استثمارك العقاري
وسكنك الفاخر مع منام
```

اجعل عبارة «استثمارك العقاري» باللون الأخضر وتحتها خط زخرفي أخضر رقيق، وبقية العنوان بلون بني/أسود دافئ. العنوان عريض جدًا، مركزي، على سطرين، مع أقصى عرض يمنع تحوله إلى ثلاثة أسطر على سطح المكتب.

### الوصف حرفيًا

```text
ندير عقارك السكني ونشغله فندقيًا بالكامل لنضمن لك أعلى عائد شهري يتجاوز ضعف الإيجار التقليدي، دون أي عناء صيانة أو تشغيل.
```

### الأزرار

- Primary أخضر: «احجز إقامتك الفاخرة» مع أيقونة مفتاح، وينتقل إلى صفحة الشقق.
- Secondary أبيض بحد خفيف: «اعرض عقارك للإدارة» مع أيقونة مبنى، وينتقل إلى صفحة الملاك والمستثمرين.

### شرائح الثقة

أسفل الأزرار ثلاث Pills بيضاء بحد وظل خفيف، بالترتيب RTL:

1. «دخول ذاتي ذكي بالكامل».
2. «إشراف وصيانة دورية شاملة».
3. «شفافية وحسابات معتمدة».

كل شريحة معها أيقونة تحقق دائرية خضراء. هذا القسم لا يستخدم بحثًا أو صورة أو إحصاءات عائمة.

## 37. قسم «في منام» والخدمات التشغيلية

Layout بعمودين متساويين تقريبًا. عمود النص والإحصاءات في اليمين، وقائمة الخدمات في اليسار.

### العمود الأيمن

Eyebrow:

```text
عن شركتنا
```

العنوان:

```text
في منام
```

الوصف:

```text
ندمج الضيافة الفاخرة بذكاء التقنية لنضمن لعقاراتكم أعلى معدل إشغال وأفضل عائد استثماري دون عناء.
```

بعده شبكة 2×2 من بطاقات الإحصاءات:

- `+120` — «وحدة عقارية فاخرة».
- `91.4%` — «متوسط نسبة الإشغال».
- `2x - 3x` — «ضعف الإيجار التقليدي».
- `+15K` — «نزيل سعيد سنويًا».

الأرقام قابلة للإدارة من Dashboard، بما في ذلك القيمة، prefix، suffix، العنوان، الأيقونة، والترتيب. لا تغير القيم المرجعية في Seed الأولي.

### العمود الأيسر

عنوان صغير:

```text
خدماتنا التشغيلية المتكاملة
```

أربع بطاقات بيضاء أفقية، أيقونتها داخل مربع أبيض/رمادي صغير في اليمين:

1. العنوان: «إدارة تشغيلية شاملة 360 درجة».
   الوصف: «استقبال للضيوف، تنظيف فندقي يومي، وإشراف وصيانة متكاملة للحفاظ على جودة العقار.»
2. العنوان: «تجهيز وتأثيث بمعايير فندقية نخبوية».
   الوصف: «نجهز عقارك بأرقى قطع الأثاث والبياضات الفاخرة ليرتقي به إلى مستوى 5 نجوم.»
3. العنوان: «تقارير شفافة أولًا بأول».
   الوصف: «متابعة دقيقة لنسب الإشغال والحجوزات والأرباح بشفافية تامة.»
   رابط نصي داخل البطاقة: «ابدأ الاستثمار معنا ←».
4. العنوان: «فريق صيانة ونظافة فوري».
   الوصف: «فريق دعم وصيانة متكامل متواجد على مدار الساعة لتلبية متطلبات الضيوف فورًا.»

## 38. قسم الوحدات المختارة في الرئيسية

Eyebrow:

```text
شقق مختارة بعناية
```

العنوان:

```text
تصفح باقة مختارة من أرقى وحداتنا السكنية
```

الوصف:

```text
وحدات مجهزة بأحدث تقنيات الدخول الذكي والخدمات الفندقية المتكاملة لتمنحك إقامة لا تُنسى.
```

اعرض ثلاث بطاقات في صف واحد. البطاقات المرجعية الأولى:

### البطاقة الأولى

- الاسم: «استوديو منام النخبة - الملقا».
- الموقع: «الملقا، شمال الرياض».
- السعر: `450 ر.س / ليلة`.
- التقييم: `4.9`.
- غرفة نوم: 1.
- دورة مياه: 1.
- الأشخاص: 2.

### البطاقة الثانية

- الاسم: «شقة منام التنفيذية - العقيق».
- الموقع: «العقيق، بالقرب من مركز الملك عبد الله المالي».
- السعر: `650 ر.س / ليلة`.
- التقييم: `4.85`.
- غرفة نوم: 1.
- دورات المياه: 2.
- الأشخاص: 3.

### البطاقة الثالثة

- الاسم: «جناح منام الملكي - الحمراء».
- الموقع: «الحمراء، بالقرب من كورنيش جدة والنافورة».
- السعر: `1100 ر.س / ليلة`.
- التقييم: `4.92`.
- غرف النوم: 2.
- دورات المياه: 2.
- الأشخاص: 5.

كل بطاقة: صورة علوية كبيرة بنسبة تقارب 4:3، السعر أعلى الصورة، التقييم أسفل الصورة في Badge أخضر مع نجمة ذهبية، ثم الموقع، الاسم، صف المواصفات، وفاصل، وزران متساويان: «تفاصيل الشقة» Secondary و«احجز الآن» Primary. أسفل الشبكة زر مركزي عريض: «عرض جميع الشقق المتوفرة».

هذه بيانات Seed فقط. العرض نفسه يأتي من Dashboard، مع `featured=true` وترتيب `featuredRank`.

## 39. قسم تقييمات النزلاء

Eyebrow:

```text
تجارب شركائنا
```

العنوان:

```text
تقييمات النزلاء وتجاربهم
```

الوصف:

```text
ماذا يقول ضيوفنا عن تجربتهم الفندقية الاستثنائية في شقق منام الفاخرة.
```

اعرض ثلاث بطاقات كبيرة. في أعلى كل بطاقة خمس نجوم خضراء وعلامة اقتباس زخرفية فاتحة. محتوى Seed:

### خالد بن محمد

```text
منام غيرت مفهومي عن الاستثمار العقاري بالكامل. كنت أؤجر شقتي سنويًا بـ 50 ألف ريال، ومع منام وإدارتهم الفندقية الذكية والتسعير الديناميكي، تجاوز عائدي الشهري الصافي حاجز الـ 110 ألف ريال سنويًا دون أي عناء صيانة أو تشغيل.
```

الصفة: «مالك عقار بالرياض».

### سارة اليوسف

```text
الشقق نظيفة بشكل يعادل بل يتفوق على فنادق الخمس نجوم! الدخول الذاتي سريع للغاية وسلس، ونظام التطهير والتعقيم الدقيق يمنحني مكانًا مريحًا ونظيفًا وثقة نفسية رائعة. ضيافتهم السعودية تثلج الصدر.
```

الصفة: «نزيلة متكررة - جدة».

### عبد الله الدوسري

```text
الشفافية هي أهم ميزة في منام. لوحة تحكم المالك تعرض لي بدقة كل حجز، تقدم المبالغ المستحقة، ونسب الإشغال والأرباح. شقة واحدة داخلتها منام تحقق لي دخلًا رائعًا ويحافظ على الشقق كأنها جديدة تمامًا.
```

الصفة: «مالك 4 وحدات بالخبر».

حافظ على النصوص كـSeed قابلة للتحرير والاعتماد والنشر من لوحة التحكم، ولا تكتبها داخل Component.

## 40. قسم الأسئلة الشائعة بالمحتوى الكامل

Eyebrow: «الإجابات الفورية».

العنوان: «الأسئلة الشائعة».

الوصف:

```text
كل ما تود معرفته عن إدارة منام لعقارك أو عن إقامتك معنا.
```

قسم الأسئلة بعمودين على سطح المكتب:

### أسئلة النزلاء والضيوف

1. **ما الذي يميز شقق «منام» الفندقية الفاخرة؟**
   الإجابة: «توفر شقق منام تجربة سكنية فندقية متكاملة تجمع بين الخصوصية والراحة والتقنية؛ وحدات مؤثثة بعناية، تنظيف وتعقيم بمعايير فندقية، دخول ذاتي ذكي، إنترنت سريع، ودعم متواصل طوال الإقامة.»
2. **هل جميع وحدات منام مرخصة رسميًا؟**
   الإجابة: «نعم، لا تُنشر أي وحدة قبل استكمال متطلبات الجاهزية والتراخيص النظامية المطلوبة، وتتم مراجعة بيانات الوحدة بصورة دورية.»
3. **كيف يتم تسجيل الدخول للوحدة الفندقية؟**
   الإجابة: «بعد تأكيد الحجز واستكمال التحقق، تصلك تعليمات الدخول ومعلومات الوحدة عبر قناة التواصل المسجلة. تستخدم الوحدات المؤهلة دخولًا ذاتيًا ذكيًا يتيح وصولًا سريعًا وآمنًا دون انتظار.»
4. **هل تتوفر خدمات تنظيف للوحدة أثناء إقامتي؟**
   الإجابة: «نعم. يشمل تجهيز الوحدة تنظيفًا وتعقيمًا قبل الوصول، ويمكن طلب تنظيف إضافي أثناء الإقامة حسب الوحدة والمدة والرسوم الموضحة قبل التأكيد.»

### أسئلة الملاك والمستثمرين

1. **كيف تضمنون سلامة عقاري ومحتويات الشقة؟**
   الإجابة: «نطبق فحص جاهزية موثقًا، ونسجل حالة الوحدة ومحتوياتها، ونستخدم ضوابط دخول ومتابعة تشغيلية وصيانة دورية. كما توضح اتفاقية الإدارة المسؤوليات وآلية معالجة أي ملاحظة أو ضرر.»
2. **كيف تفوق عوائد منام للإيجار السنوي التقليدي؟**
   الإجابة: «من خلال التشغيل الفندقي، والتسعير الديناميكي حسب المواسم والطلب، والتوزيع عبر قنوات الحجز، ورفع مستوى الجاهزية والتقييمات. العائد المتوقع يختلف حسب المدينة والموقع ونوع الوحدة ولا يعد ضمانًا ثابتًا.»
3. **هل يمكنني متابعة تفاصيل أداء عقاري لحظة بلحظة؟**
   الإجابة: «نعم. تتيح منظومة التقارير متابعة الحجوزات ونسب الإشغال والإيرادات والمصروفات وحالة التحويلات، مع كشوف دورية واضحة.»
4. **ما هي شروط انضمام عقاري إلى منصة منام؟**
   الإجابة: «ملكية أو تفويض نظامي ساري، موقع ضمن نطاق الخدمة، سلامة إنشائية وخدمات مكتملة، مطبخ وتجهيزات أساسية، وقابلية الوحدة لاستيفاء معايير التأثيث والتشغيل الفندقي.»
5. **كم تبلغ رسوم الإدارة والتشغيل في منام؟**
   الإجابة: «يحدد نموذج العرض الرسوم حسب نطاق الإدارة والخدمات وحالة الوحدة. النموذج المرجعي يتراوح بين 20% و25% من إجمالي الدخل الشهري، وتظهر النسبة النهائية بوضوح في العرض والعقد.»
6. **متى وكيف أستلم أرباح عقاري الشهرية؟**
   الإجابة: «يصدر كشف الأداء بعد إقفال الدورة المالية، ويحول صافي الاستحقاق إلى الحساب البنكي المعتمد وفق الموعد المحدد في الاتفاقية؛ النموذج المرجعي قبل اليوم العاشر من كل شهر ميلادي.»
7. **هل يوجد عقد تشغيل رسمي؟ وما هي مدته؟**
   الإجابة: «نعم، توقع اتفاقية إدارة وتشغيل توضح النطاق والرسوم والمسؤوليات وآلية الإنهاء. المدة المرجعية سنة ميلادية قابلة للتجديد، وتحدد النسخة النهائية في العقد.»
8. **هل أستطيع استخدام شقتي لإقامتي الشخصية؟**
   الإجابة: «يمكن تنظيم استخدام المالك مسبقًا وفق بنود الاتفاقية وحالة الحجوزات، مع حجز الأيام المطلوبة عبر القناة المخصصة حتى لا تتعارض مع التزامات الضيوف.»

كل الأسئلة والأجوبة Dynamic مع فئة وترتيب وحالة نشر.

## 41. شركاء النجاح

Eyebrow باللغة الإنجليزية كما هو: `SUCCESS PARTNERS & CERTIFIED PLATFORMS`.

العنوان:

```text
منظومة شركاء النجاح والمنصات المعتمدة
```

اعرض خمسة شعارات في بطاقات بيضاء منخفضة التباين: Booking.com، Airbnb، وزارة السياحة، Gathern، ثم منصة حجز/شريك خامس من إعدادات Dashboard. لا تكرر Booking.com مرتين في النسخة النهائية إلا إذا كانت البيانات الفعلية تقصد كيانين مختلفين. الشعارات Grayscale افتراضيًا ويمكن أن تستعيد لونها عند Hover. لا تعرض شريكًا دون شعار واسم وalt صالحين.

## 42. Footer بالمحتوى الكامل

الخلفية المرئية تستمر بنفس lattice pattern فوق سطح فاتح، مع فاصل علوي. لا تحوّل Footer إلى شريط داكن إذا كان اللون الحالي في المشروع فاتحًا؛ اعتمد القيمة الدقيقة الموجودة في التنفيذ. ضع أربعة أعمدة RTL:

### منام

استخدم `/dark-logo.png`، ثم:

```text
شركة منام لإدارة وتشغيل الوحدات السكنية والسياحية الفاخرة بالمملكة. نسخر التقنية الحديثة وخبرات الضيافة العالمية لنصنع تجربة سكنية واستثمارية استثنائية.
```

أزرار: WhatsApp، LinkedIn، TikTok، Instagram، X.

### روابط سريعة

- «لماذا منام؟».
- «للملاك والمستثمرين».
- «للنزلاء والضيوف».
- «الأسئلة الشائعة».

### مكتبنا الرئيسي

```text
المملكة العربية السعودية، الرياض، حي الملقا، طريق الملك سلمان
info@usemanam.com
+966 53 872 1499
```

### النشرة البريدية الاستثمارية

```text
اشترك معنا للحصول على آخر التقارير العقارية وفرص الاستثمار الحصرية.
```

حقل: «البريد الإلكتروني»، وزر إرسال بأيقونة طائرة ورقية.

### الشريط السفلي

```text
جميع الحقوق محفوظة لشركة منام لإدارة وتشغيل الوحدات السكنية © 2026
```

روابط: «الشروط والأحكام»، «سياسة الخصوصية».

السطر النظامي:

```text
شركة منام لإدارة وتشغيل المرافق - سجل تجاري رقم: 1010834279
مرخصة من وزارة السياحة بترخيص تشغيل رقم: 41002345
العنوان الوطني: 7384 طريق الملك سلمان، حي الملقا، الرياض 13524، المملكة العربية السعودية
```

جميع البيانات من Site Settings، والقيم أعلاه Seed أولي. سنة الحقوق ديناميكية.

## 43. النوافذ القانونية كما يجب أن تظهر

عند النقر على «سياسة الخصوصية»، Blur قوي وOverlay أبيض شبه شفاف فوق الصفحة، وDialog أبيض مركزي بعرض يقارب 600px. العنوان: «سياسة الخصوصية وحماية البيانات». Alert علوي:

```text
نحترم في شركة منام خصوصيتك ونلتزم بحماية بياناتك الشخصية وفقًا للأنظمة المعمول بها في المملكة العربية السعودية.
```

اعرض محتوى كاملًا على الأقل بالعناوين التالية: «جمع البيانات»، «استخدام المعلومات»، «مشاركة البيانات»، «حماية البيانات»، «مدة الاحتفاظ»، «حقوقك»، «ملفات الارتباط»، «تحديث السياسة»، «التواصل معنا». لا تكتفِ بالثلاث فقرات الظاهرة أولًا؛ Dialog قابل للتمرير وله Footer ثابت بزر «إغلاق».

عند النقر على «الشروط والأحكام»، افتح Dialog مطابقًا بعنوان «الشروط والأحكام»، ومحتوى كاملًا يتضمن: قبول الشروط، الأهلية، الحجوزات، الدفع، التأمين، الإلغاء والاسترداد، الدخول والمغادرة، مسؤولية النزيل، الأضرار، الخدمات، القوة القاهرة، الملكية الفكرية، حدود المسؤولية، القانون والاختصاص، والتواصل.

هناك وثيقة ثالثة خاصة بالملاك بعنوان «شروط الجاهزية والتعاقد لشركاء منام»، تفتح من صفحة الملاك وتتضمن النص التالي كـSeed:

1. **ملكية العقار وموقعه**: «يجب تقديم صك ملكية إلكتروني ساري المفعول أو وكالة شرعية رسمية لإدارة العقار. أن يقع العقار في المدن التي تغطيها خدماتنا حاليًا (الرياض، جدة، الخبر). أن يكون العقار في حي مكتمل الخدمات (طرق معبدة، إنارة، مياه، صرف صحي).»
2. **جاهزية الوحدة**: «أن تكون الوحدة بحالة إنشائية ممتازة وخالية من العيوب الظاهرة (تسربات، شقوق، رطوبة). تركيب مكيفات سبليت أو مركزية بحالة ممتازة في جميع الغرف. توفير مطبخ مركب بالكامل.»
3. **التأثيث والتجهيز (في حال الإدارة الشاملة)**: «استخدام مفروشات فندقية معتمدة (مراتب سرير طبية، بياضات قطنية بيضاء). توفير شاشة ذكية Smart TV واتصال إنترنت فايبر عالي السرعة. توفير دخول ذكي (قفل ذكي) للباب الرئيسي. يمكن لفريق منام الإشراف على التأثيث مقابل رسوم إضافية.»
4. **الرسوم والعوائد**: «نسبة الإدارة التشغيلية تتراوح بين 20% إلى 25% من إجمالي الدخل الشهري. يتم تحويل الأرباح للمالك في موعد أقصاه يوم 10 من كل شهر ميلادي. فترة التعاقد الأدنى سنة ميلادية واحدة قابلة للتجديد.»

## 44. صفحة تصفح الشقق: البنية والنصوص

Hero الصفحة بلا صورة، مركزي، بالنص:

Eyebrow: «للنزلاء والضيوف».

العنوان:

```text
فخامة وخدمة فندقية راقية، ودفء وخصوصية البيت
```

الوصف:

```text
شقق فندقية فاخرة ومجهزة بالكامل في أرقى الأحياء. دخول ذاتي ذكي، إنترنت فايبر سريع، خدمات صيانة دورية وضيافة سعودية أصيلة.
```

ثلاث بطاقات أفقية أسفل الوصف:

- «نظافة وتعقيم بمعايير فندقية».
- «دخول ذاتي ذكي وسريع».
- «أحياء حيوية ونخبوية بالقرب منك».

بعدها شريط فلترة أبيض كبير. في اليمين Tabs المدن بالترتيب: «كل المدن»، «الرياض»، «جدة»، «الخبر». في اليسار حقل بحث Placeholder:

```text
البحث عن شقة (مثال: الملقا، العقيق...)
```

فلترة المدينة والبحث تعملان معًا، وتُحفظان في URL. تحت الشريط شبكة ثلاثية للشقق.

## 45. Seed كامل لبطاقات صفحة الشقق

إضافة إلى الوحدات الثلاث الأولى، استخدم:

### بنتهاوس منام المطل على البحر - الشاطئ

- الموقع: «الشاطئ، إطلالة بانورامية على البحر الأحمر».
- السعر: `1800 ر.س / ليلة`.
- التقييم: `4.97`.
- 3 غرف نوم، 3 دورات مياه، 6 أشخاص.
- المزايا: «إطلالة بانورامية كاملة»، «تراس خارجي فسيح»، «نظام تحكم ذكي بالمنزل».

### شقة منام البوتيك - الحزام الذهبي

- الموقع: «الحزام الذهبي، الخبر».
- السعر: `550 ر.س / ليلة`.
- التقييم: `4.78`.
- غرفة نوم واحدة، دورة مياه واحدة، شخصان.
- المزايا: «موقع هادئ ونخبوي»، «تكييف مركزي ذكي»، «دخول إلكتروني ذكي».

### شقة منام العائلية الفاخرة - كورنيش الخبر

- الموقع: «كورنيش الخبر، دقيقة من البحر».
- السعر: `850 ر.س / ليلة`.
- التقييم: `4.88`.
- غرفتا نوم، دورتا مياه، 4 أشخاص.
- المزايا: «خطوات من كورنيش الخبر»، «صالة جلوس فسيحة جدًا»، «دخول ذاتي ذكي وسريع».

لا تتوقف الشبكة عند 6 عناصر. أنشئ بيانات اختبار كافية للـPagination، لكن اجعل هذه الست أول Seed منشور.

## 46. Modal تفاصيل الشقة

عند الضغط على «تفاصيل» افتح Dialog رأسيًا كبيرًا بعرض يقارب 760–800px، مع صورة Cover كبيرة في الأعلى وClose دائري أعلى الزاوية. Blur للخلفية. لا تنتقل مباشرة إذا كان المطلوب Quick View، لكن وفّر URL أو رابطًا للتفاصيل الكاملة.

محتوى الوحدة المرجعية:

- التقييم `4.85`.
- Label إنجليزي صغير: `RIYADH` أو المدينة من البيانات.
- العنوان: «شقة منام التنفيذية - العقيق».
- الموقع: «العقيق، بالقرب من مركز الملك عبد الله المالي».
- «1 غرفة نوم»، «2 دورة مياه»، «3 أشخاص».
- عنوان: «الوصف التفصيلي».
- النص: «شقة مميزة للأعمال دقائق من مركز الملك عبدالله المالي (KAFD)، صالة أنيقة، مرتبة فندقية، ومكتب عمل متكامل.»
- عنوان: «المزايا الفندقية المرفقة».
- المزايا: «قريب من مركز KAFD»، «مكتب عمل مجهز»، «دخول ذاتي ذكي»، «غسالة ملابس متكاملة»، «ضيافة فاخرة».
- السعر: «السعر لكل ليلة» ثم `SAR 650`.
- زر: «احجز الآن».

اجعل Footer السعر والزر Sticky داخل Dialog عند المحتوى الطويل.

## 47. Modal تأكيد الحجز

العنوان: «تأكيد حجز وحدتك السكنية».

السطر الثانوي يعرض الوحدة ديناميكيًا، مثل:

```text
الوحدة: شقة منام التنفيذية - العقيق
```

الحقول بالترتيب:

1. «الاسم الكريم»، Placeholder: «مثال: عبد العزيز الرويلي».
2. «رقم الجوال»، Placeholder: `+966 5x xxx xxxx`.
3. «تاريخ الدخول».
4. «تاريخ المغادرة».
5. «عدد النزلاء» مع Stepper ناقص/زائد وقيمة مثل «1 نزيل».

شريط ضمان:

```text
ضمان الدخول الذكي والدفع الإلكتروني المعتمد 100%
```

زر الإرسال:

```text
تأكيد الحجز والدفع الذكي
```

الزر لا يدعي تنفيذ دفع حقيقي ما لم توجد بوابة دفع مربوطة. عند غيابها غيّر سلوكه إلى إنشاء طلب حجز وحالة Pending، مع رسالة واضحة بأن الفريق سيؤكد الطلب.

## 48. صفحة الملاك والمستثمرين: البنية المرجعية

في أعلى الصفحة Layout من عمودين:

### العمود الأيمن

Eyebrow: «للملاك والمستثمرين».

العنوان:

```text
ضاعف عوائد عقارك دون أي عناء تشغيلي
```

الوصف:

```text
ندير عقارك السكني ونشغله فندقيًا بالكامل لنضمن لك أعلى عائد شهري يتجاوز ضعف الإيجار التقليدي، دون أي عناء صيانة أو تشغيل.
```

بطاقتان:

- «عائد شهري يتجاوز ضعف الإيجار السنوي».
- «إدارة تشغيلية فندقية شاملة (360 درجة)».

### العمود الأيسر: شروط الجاهزية

بطاقة كبيرة بعنوان:

```text
شروط الجاهزية والتعاقد لشركاء منام
```

الوصف:

```text
يرجى الاطلاع والموافقة على شروط قبول الوحدات قبل البدء بعملية التسجيل.
```

Alert بعنوان «ملاحظة هامة» ونص: «نحرص في منام على جودة الوحدات لضمان أعلى عائد للمالك وأفضل تجربة للضيف.» ثم الأقسام الأربعة المذكورة في قسم الشروط. المحتوى داخل منطقة Scroll عند الحاجة، وزر ثابت:

```text
موافق، ابدأ التسجيل ←
```

قبل الموافقة لا يظهر النموذج. بعد الموافقة يتحول العمود الأيسر إلى نموذج التسجيل مع بقاء العمود الأيمن ثابتًا.

## 49. نموذج انضمام الملاك وحالة النجاح

عنوان النموذج:

```text
انضم إلى شركاء نجاح منام
```

الوصف:

```text
سجل تفاصيل عقارك الآن وسيقوم مستشارنا العقاري بالتواصل معك لتقديم دراسة عوائد مجانية مخصصة لعقارك.
```

ضع رابطًا صغيرًا «الشروط والجاهزية» يعيد فتح الشروط. الحقول:

- «الاسم الكريم»، Placeholder: «الاسم الثلاثي الكريم».
- «رقم الجوال»، Placeholder: `05xxxxxxxx`.
- «عدد الوحدات العقارية» مع Stepper يبدأ من 1.
- «المدينة»، Placeholder: «مثال: الرياض، الدمام».
- «نوع الوحدة العقارية»، Placeholder: «مثال: شقة، بنتهاوس، فيلا».

زر كامل العرض:

```text
إرسال وتجهيز دراسة العوائد
```

بعد النجاح استبدل النموذج داخل البطاقة بحالة نجاح، لا تفتح Toast فقط:

- دائرة كبيرة بأيقونة تحقق.
- العنوان: «شكرًا لانضمامك لشركاء منام!».
- الوصف: «تم إرسال طلبك بنجاح! سيتواصل معك مستشار منام العقاري قريبًا.»
- زر: «تقديم طلب لوحدة أخرى» يعيد النموذج نظيفًا.

## 50. العناصر العائمة

في جميع الصفحات العامة:

- زر محادثة دائري أخضر في أسفل اليسار، مع نقطة حالة خضراء صغيرة. يفتح Widget أو WhatsApp/دعم حقيقي من Site Settings؛ لا تستخدم زرًا ميتًا.
- فوقه زر «العودة للأعلى» أبيض مربع ناعم يظهر بعد التمرير.
- احترم Safe Areas والموبايل، ولا تغطِّ زر الحجز الثابت أو Footer links.

## 51. قواعد التحويل إلى Light Mode فقط

التصميم المرجعي قد يتضمن زر قمر، لكن النسخة المطلوبة لا تحتوي Dark Mode. لذلك:

- احذف Toggle من Header.
- احذف `dark:` classes وTheme provider والتخزين المحلي الخاص بالوضع إذا لم يكن مستخدمًا لشيء آخر.
- جميع الحوارات والبطاقات والخلفيات تبقى فاتحة.
- اللون الأخضر الداكن يستخدم للأزرار والعناوين الجزئية وFooter accents، لا كخلفية صفحة كاملة.
- اختبر أن النظام لا يتحول تلقائيًا إلى مظلم عبر `prefers-color-scheme`.

## 52. نموذج المحتوى المطلوب في Dashboard

يجب أن يدعم Dashboard تحرير المحتوى الحرفي السابق دون تعديل الكود:

```ts
type PageContent = {
  key: 'home' | 'apartments' | 'owners';
  locale: 'ar';
  sections: Array<{
    id: string;
    type: string;
    eyebrow?: string;
    title: string;
    description?: string;
    enabled: boolean;
    sortOrder: number;
    payload: Record<string, unknown>;
  }>;
};

type SiteSettings = {
  logoPath: '/dark-logo.png';
  contactEmail: string;
  contactPhone: string;
  officeAddress: string;
  socialLinks: Array<{ platform: string; url: string; enabled: boolean }>;
  commercialRegistration?: string;
  tourismLicense?: string;
  nationalAddress?: string;
  chatProvider?: 'whatsapp' | 'custom' | 'disabled';
  chatUrl?: string;
};
```

الـDashboard يحتاج أيضًا:

- CRUD كامل للشقق مع معرض الصور والسعر والمزايا والمدينة والتقييم والسعة والترتيب والـFeatured.
- CRUD للتعليقات مع الموافقة والنشر والنجوم والترتيب.
- CRUD للأسئلة مع فئة «ضيف» أو «مالك» وترتيب وحالة نشر.
- CRUD للشركاء والشعارات.
- محرر Footer وبيانات الشركة.
- محرر الوثائق القانونية مع Version و`lastUpdatedAt`.
- محرر شروط الجاهزية ورسوم الإدارة والتنبيهات.
- عرض Leads للحجز والملاك مع Status history وExport آمن.

## 53. قائمة مطابقة بصرية إضافية

- [ ] الخلفية lattice تغطي Hero وكل المساحات بين الأقسام وحتى Footer، opacity 20%.
- [ ] Hero الرئيسية نصي مركزي بلا صورة.
- [ ] العنوان الرئيسي مطابق حرفيًا مع تمييز «استثمارك العقاري» بالأخضر.
- [ ] قسم «في منام» عمودان: خدمات يسار، نص وإحصاءات يمين.
- [ ] بطاقات الشقق ثلاثية وبها السعر فوق الصورة والتقييم أسفلها.
- [ ] بطاقات التعليقات ثلاثية كبيرة بنجوم وعلامة اقتباس.
- [ ] FAQ عمودان حسب نوع المستخدم.
- [ ] Footer فاتح ومفصل لا Footer داكن عام.
- [ ] صفحة الشقق تبدأ بعنوان الخدمة الفندقية وثلاث مزايا ثم Tabs المدن والبحث.
- [ ] التفاصيل والحجز Dialogs مركزية مع Blur قوي.
- [ ] صفحة الملاك تبدأ بالشروط، ثم النموذج، ثم Success State داخل البطاقة نفسها.
- [ ] لا يوجد Dark Mode أو زر قمر.
- [ ] كل شعار في الموقع يستخدم `/dark-logo.png` فقط، باستثناء شعارات الشركاء التي تمثل شركاء خارجيين.

هذا الملحق لا يطلب نسخ أخطاء أو تناقضات بيانات تجريبية. أصلح التكرار، اجعل العربية سليمة، وحافظ على الشكل والترتيب والمحتوى التجاري نفسه. نفّذ المحتوى كـSeed قابل للإدارة، لا ككتل نصية صلبة داخل الواجهة.
